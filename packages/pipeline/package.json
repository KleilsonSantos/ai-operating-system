{
  "name": "@aios/pipeline",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Contrato estável do núcleo AIOS — runPipeline para CLI/integradores.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"process.exit(0)\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@aios/shared": "workspace:*",
    "@aios/intent": "workspace:*",
    "@aios/policy": "workspace:*",
    "@aios/context": "workspace:*",
    "@aios/orchestration": "workspace:*",
    "@aios/quality-gate": "workspace:*",
    "@aios/workspace": "workspace:*",
    "@aios/knowledge": "workspace:*",
    "@aios/memory": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
