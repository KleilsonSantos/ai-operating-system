# {{DISPLAY_NAME}}

AIOS agent plugin scaffolded with `@aios/create-agent` (Phase 5 / ADR-0023).

## Manifest

See `agent.yaml` for name, version, inputs, outputs, and engine dependencies.

## Develop

```bash
pnpm install
pnpm test
pnpm typecheck
```

## Register locally

From an AIOS workspace root (with `@aios/agent-registry`):

```bash
# save path for discovery — see aios list-agents / aios_list_agents
```

Guide: [Writing an Agent](https://github.com/KleilsonSantos/ai-operating-system/blob/sandbox/docs/guides/writing-an-agent.md)
