{
  "name": "@aios/operational-state",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Operational State — unified light control-plane snapshot (#84).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"process.exit(0)\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@aios/shared": "workspace:*",
    "@aios/status": "workspace:*",
    "@aios/workspace": "workspace:*",
    "@aios/governance": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
