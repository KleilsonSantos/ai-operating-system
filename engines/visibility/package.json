{
  "name": "@aios/visibility",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Visibility Plane — correlate governance fragments + opt-in Obsidian export (ADR-0030).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"process.exit(0)\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@aios/knowledge": "workspace:*",
    "@aios/operational-state": "workspace:*",
    "@aios/policy": "workspace:*",
    "@aios/shared": "workspace:*",
    "@aios/status": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.9.2",
    "vitest": "^4.1.11"
  }
}
