/** Decision Engine — Decide se um agente/plugin precisa participar. */
export function shouldRunAgent(agentId: string, intentKind: string): boolean {
  void agentId
  void intentKind
  return true
}
