{
  "name": "@aios/status",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Governance status — health + attention for console (#71).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"process.exit(0)\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@aios/shared": "workspace:*",
    "@aios/workspace": "workspace:*",
    "@aios/policy": "workspace:*",
    "@aios/memory": "workspace:*",
    "@aios/provider": "workspace:*",
    "@aios/governance": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
