{
  "name": "@aios/agent-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "Documentation Agent — plugin AIOS",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"process.exit(0)\"",
    "test": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "@aios/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
