{
  "name": "@aios/agent-qa",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "description": "QA Agent — plugin AIOS",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "node -e \"process.exit(0)\"",
    "test": "node -e \"process.exit(0)\""
  },
  "dependencies": {
    "@aios/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
